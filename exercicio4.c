#include stdio.h
#include math.h

int main(void)
/*ae(t)as(w)*/
{
 float t, w, y, x;
 printf(entre com o valor de T );
 scanf(%f, &t);
 y = t2+4;
 x = 2t-4;
 w = 2t+4y-3x;
 printf(o valor de W sera de %.2F, w);
 
    
return 0;
}